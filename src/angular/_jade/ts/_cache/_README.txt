Cached files that are no longer used directly via Jade extends clauses:

  guide/attribute-directives.jade
  guide/displaying-data.jade
  guide/index.jade
  guide/learning-angular.jade

We're keeping these files so as to make it easier to find out what the
differences are between our base of these files and the most recent
angular.io updates.
